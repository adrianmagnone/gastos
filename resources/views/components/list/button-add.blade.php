<a href="{{ url($url) }}" class="btn btn-primary d-none d-sm-inline-block">
    <i class="icon ti ti-plus"></i>
    {{ $text }}
</a>
<a href="{{ url($url) }}" class="btn btn-primary d-sm-none btn-icon" aria-label="{{ $text }}">
    <i class="icon ti ti-plus"></i>
</a>